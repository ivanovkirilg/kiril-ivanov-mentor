/*
5) Създайте функция reverseBit. Тя получава следните
параметри - число и индекс. Като резултат функцията
обръща бита на съответната позиция в числото
(съответно от 0 на 1 или от 1 на 0).
*/

// reverseBit()
//      create a mask by shifting
//          (it should have just the bit at the specified index set)
//      choose the bitwise operation that will (always) change only this bit
//      return the result

// main()
//      print the result of setBit() with:
//          number   bit index      expected result
//          0        0              1
//          1        0              0
//          0        1              2
//      print the result of reverseBit( reverseBit(someNum, someBit, sameBit) )
